import React from 'react';

const Button: React.FC = () => {
  return (
    <button className="text-gray-600 hover:bg-gray-700 hover:text-white p-2 rounded-2xl">
      회원가입
    </button>
  );
};

export default Button;